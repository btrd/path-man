syntax = "proto3";

package pathman;

service AdnExchange {
  rpc Shuffle(stream Adn) returns (stream Adn) {}
}

message Adn {
  int32 direct = 1;
  repeated int32 steps = 2;
  int32 id_village = 3;
}
